services:
  backend:
    build: .
    container_name: smart_construction_backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - JWT_SECRET=${JWT_SECRET}
      - PROJECT_NAME=${PROJECT_NAME:-Smart Construction}
      - PORT=8000
    env_file:
      - .env
